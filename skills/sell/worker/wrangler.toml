# Cloudflare Worker configuration for Vibes Sell
# See: https://developers.cloudflare.com/workers/wrangler/configuration/

name = "vibes-sell-api"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables - update these for your app
[vars]
PAGES_DOMAIN = "your-project.pages.dev"
ALLOWED_ORIGIN = "*"

# Secret variables (set via `wrangler secret put CLERK_SECRET_KEY`)
# - CLERK_SECRET_KEY

# Routes - add TWO routes after deploying via Cloudflare dashboard:
# 1. *.yourdomain.com/* (subdomains)
# 2. yourdomain.com/* (root domain for API access)
